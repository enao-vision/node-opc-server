{
  "name": "opc-ua-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "server": "node ./src/server.js",
    "client": "node ./src/client.js",
    "dev": "node --inspect --watch-path=./src",
    "docker:build": "docker build -t opc-ua-server .",
    "docker:run": "docker run --detach -p 4334:4334 opc-ua-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "node-opcua": "^2.153.0",
    "onoff": "^6.0.3"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "^3.5.3"
  }
}
